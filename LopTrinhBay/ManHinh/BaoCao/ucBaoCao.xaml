<UserControl x:Class="QuanLiSanCauLong.LopTrinhBay.ManHinh.BaoCao.UcBaoCao"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="1080" d:DesignHeight="700"
             Background="#F6FDF9">

    <!-- ============ RESOURCES (bắt buộc) ============ -->
    <UserControl.Resources>
        <SolidColorBrush x:Key="CardBg" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="CardBorder" Color="#E7ECEB"/>

        <!-- Card style -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="Background" Value="{StaticResource CardBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource CardBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Margin" Value="0,0,16,0"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.05"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Text styles -->
        <Style x:Key="KpiLabel" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#6B7E74"/>
            <Setter Property="FontSize" Value="12"/>
        </Style>
        <Style x:Key="KpiValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#21382D"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="22"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <StackPanel Orientation="Vertical" DockPanel.Dock="Left">
                <TextBlock Text="Báo cáo &amp; Thống kê" FontSize="22" FontWeight="Bold" Foreground="#21382D"/>
                <TextBlock Text="Phân tích doanh thu và hiệu suất kinh doanh" Foreground="#6B7E74" FontSize="12"/>
            </StackPanel>

            <StackPanel Orientation="Horizontal" DockPanel.Dock="Right" HorizontalAlignment="Right">
                <ComboBox Width="140" ItemsSource="{Binding TimeRanges}"
                          SelectedItem="{Binding SelectedTimeRange}"
                          HorizontalAlignment="Right" VerticalAlignment="Center"
                          Margin="0,0,8,0"/>
                <Border Padding="8" Background="#F0F7F5" CornerRadius="12" Margin="0,0,8,0">
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="20" Height="20" Fill="#0FBF84" Margin="0,0,6,0"/>
                        <TextBlock Text="{Binding CurrentUserDisplay}" Foreground="#21382D"/>
                    </StackPanel>
                </Border>
                <Button Content="Xuất Excel" Padding="12,6" Command="{Binding ExportExcelCommand}"
                        Cursor="Hand" Background="#0FBF84" BorderBrush="#C5F3DF" />
            </StackPanel>
        </DockPanel>

        <!-- KPI cards -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource Card}" Grid.Column="0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Tổng doanh thu" Style="{StaticResource KpiLabel}"/>
                        <TextBlock Text="{Binding TotalRevenueDisplay}" Style="{StaticResource KpiValue}" Margin="0,2,0,4"/>
                        <TextBlock Text="{Binding TotalRevenueDelta}" Foreground="#6B7E74" FontSize="11"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#E8FBF2" CornerRadius="12" Padding="10" Margin="8,0,0,0" VerticalAlignment="Top">
                        <TextBlock Text="💵" FontSize="16"/>
                    </Border>
                </Grid>
            </Border>

            <Border Style="{StaticResource Card}" Grid.Column="1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Doanh thu sân" Style="{StaticResource KpiLabel}"/>
                        <TextBlock Text="{Binding CourtRevenueDisplay}" Style="{StaticResource KpiValue}" Margin="0,2,0,4"/>
                        <TextBlock Text="{Binding CourtRevenuePercent}" Foreground="#6B7E74" FontSize="11"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#ECF3FF" CornerRadius="12" Padding="10" Margin="8,0,0,0" VerticalAlignment="Top">
                        <TextBlock Text="📈" FontSize="16"/>
                    </Border>
                </Grid>
            </Border>

            <Border Style="{StaticResource Card}" Grid.Column="2">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Doanh thu POS" Style="{StaticResource KpiLabel}"/>
                        <TextBlock Text="{Binding POSRevenueDisplay}" Style="{StaticResource KpiValue}" Margin="0,2,0,4"/>
                        <TextBlock Text="{Binding POSPercentOfTotal}" Foreground="#6B7E74" FontSize="11"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#FFF3E6" CornerRadius="12" Padding="10" Margin="8,0,0,0" VerticalAlignment="Top">
                        <TextBlock Text="🧃" FontSize="16"/>
                    </Border>
                </Grid>
            </Border>

            <Border Style="{StaticResource Card}" Grid.Column="3">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel>
                        <TextBlock Text="Lượt khách" Style="{StaticResource KpiLabel}"/>
                        <TextBlock Text="{Binding CustomerCountDisplay}" Style="{StaticResource KpiValue}" Margin="0,2,0,4"/>
                        <TextBlock Text="{Binding CustomerAvgPerDay}" Foreground="#6B7E74" FontSize="11"/>
                    </StackPanel>
                    <Border Grid.Column="1" Background="#F5ECFF" CornerRadius="12" Padding="10" Margin="8,0,0,0" VerticalAlignment="Top">
                        <TextBlock Text="👥" FontSize="16"/>
                    </Border>
                </Grid>
            </Border>
        </Grid>

        <!-- Chart -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="0,0,0,12" Padding="0">
            <Grid Margin="16">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Orientation="Vertical" Grid.Row="0">
                    <TextBlock Text="Biểu đồ doanh thu" FontWeight="Bold" Foreground="#21382D"/>
                    <TextBlock Text="Doanh thu theo ngày (7 ngày qua)" Foreground="#6B7E74" FontSize="11"/>
                </StackPanel>

                <Grid Grid.Row="1" Margin="0,8,0,8">
                    <Canvas x:Name="ChartCanvas" Background="Transparent"/>
                </Grid>

                <StackPanel Grid.Row="2" Orientation="Horizontal">
                    <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                        <Ellipse Width="10" Height="10" Fill="#FFB74D" Margin="0,0,6,0"/>
                        <TextBlock Text="Doanh thu POS" FontSize="11" Foreground="#6B7E74"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,16,0">
                        <Ellipse Width="10" Height="10" Fill="#4CAF50" Margin="0,0,6,0"/>
                        <TextBlock Text="Doanh thu sân" FontSize="11" Foreground="#6B7E74"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="10" Height="10" Fill="#2196F3" Margin="0,0,6,0"/>
                        <TextBlock Text="Tổng doanh thu" FontSize="11" Foreground="#6B7E74"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Titles row -->
        <Grid Grid.Row="3" Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Công suất sử dụng sân" FontWeight="Bold" Foreground="#21382D" Grid.ColumnSpan="2" Margin="10,0,574,0"/>
            <TextBlock Grid.Column="1" Text="Tổng hợp chi tiết" FontWeight="Bold" Foreground="#21382D" Margin="24,-5,-24,5"/>
        </Grid>

        <!-- Bottom content -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource Card}" Grid.Column="0">
                <ItemsControl ItemsSource="{Binding CourtUtilizations}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,4">
                                <DockPanel>
                                    <TextBlock Text="{Binding CourtName}" DockPanel.Dock="Left" Width="160"/>
                                    <TextBlock Text="{Binding UtilPercent, StringFormat={}{0}%}" DockPanel.Dock="Right" Foreground="#6B7E74"/>
                                </DockPanel>
                                <ProgressBar Value="{Binding UtilPercent}" Height="8" Minimum="0" Maximum="100"/>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Border>

            <Border Style="{StaticResource Card}" Grid.Column="1" Margin="12,0,0,0">
                <DataGrid ItemsSource="{Binding SummaryRows}" AutoGenerateColumns="False" HeadersVisibility="Column"
                          CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Chỉ tiêu" Binding="{Binding Title}" Width="*"/>
                        <DataGridTextColumn Header="Giá trị" Binding="{Binding Value}" Width="200"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Grid>
</UserControl>

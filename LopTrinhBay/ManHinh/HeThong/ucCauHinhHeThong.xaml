<UserControl x:Class="QuanLiSanCauLong.LopTrinhBay.ManHinh.HeThong.ucCauHinhHeThong"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="760" d:DesignWidth="1280"
             Background="#F6FDF9">

    <UserControl.Resources>
        <!-- Palette -->
        <SolidColorBrush x:Key="ColText"   Color="#21382D"/>
        <SolidColorBrush x:Key="ColSub"    Color="#6B7E74"/>
        <SolidColorBrush x:Key="ColBorder" Color="#E6EFEA"/>
        <SolidColorBrush x:Key="ColCard"   Color="White"/>

        <!-- Card -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource ColCard}"/>
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Padding" Value="14"/>
            <Setter Property="BorderBrush" Value="{StaticResource ColBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <!-- Section (như ảnh: nền pastel) -->
        <Style x:Key="Section" TargetType="Border" BasedOn="{StaticResource Card}">
            <Setter Property="Padding" Value="16"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>

        <!-- Input -->
        <Style TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Background" Value="#F6FDF9"/>
            <Setter Property="BorderBrush" Value="{StaticResource ColBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,6,0,12"/>
            <Setter Property="Padding" Value="8,4"/>
        </Style>

        <!-- Tab-like buttons trên cùng -->
        <Style x:Key="Pill" TargetType="ToggleButton">
            <Setter Property="Margin" Value="6,0,0,0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource ColBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border CornerRadius="18"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}">
                            <ContentPresenter Margin="6,0" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter Property="Background" Value="#E6F9F2"/>
                    <Setter Property="BorderBrush" Value="#0FBF84"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Title -->
            <RowDefinition Height="Auto"/>
            <!-- Tabs -->
            <RowDefinition Height="*"/>
            <!-- Content -->
            <RowDefinition Height="Auto"/>
            <!-- Save -->
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Cài đặt Hệ thống" FontSize="18" FontWeight="SemiBold" Foreground="{StaticResource ColText}"/>
            <TextBlock Text="Cấu hình tham số và phân quyền" Foreground="{StaticResource ColSub}" FontSize="12"/>
        </StackPanel>

        <!-- Tabs (chỉ minh họa giống ảnh; tab chính đang chọn là Cấu hình hệ thống) -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Padding="10" Background="White">
            <StackPanel Orientation="Horizontal">
                <ToggleButton IsChecked="True" Style="{StaticResource Pill}" Checked="ToggleButton_Checked">⚙  Cấu hình hệ thống</ToggleButton>
                <ToggleButton Style="{StaticResource Pill}">🔐  Phân quyền</ToggleButton>
                <ToggleButton Style="{StaticResource Pill}">🗒  Nhật ký</ToggleButton>
                <ToggleButton Style="{StaticResource Pill}">💾  Sao lưu</ToggleButton>
            </StackPanel>
        </Border>

        <!-- Content grid 2 cột -->
        <Grid Grid.Row="2" Margin="0,10,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>



            <!-- Đặt sân & Cảnh báo (vàng nhạt) -->
            <Border Grid.Column="0" Style="{StaticResource Section}" Background="#FFF8DD" Margin="0,10,0,0">
                <StackPanel>
                    <TextBlock Text="⚙  Đặt sân &amp; Cảnh báo" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="{StaticResource ColText}"/>
                    <TextBlock Text="Cảnh báo No-show (phút)" Foreground="{StaticResource ColSub}" />
                    <TextBox Text="{Binding CanhBaoNoShow}" />
                    <TextBlock Text="Số sân tối đa / lần đặt" Foreground="{StaticResource ColSub}" />
                    <TextBox Text="{Binding SoSanToiDa}" />
                    <TextBlock Text="Số slot tối đa / lần đặt" Foreground="{StaticResource ColSub}" />
                    <TextBox Text="{Binding SoSlotToiDa}" />
                </StackPanel>
            </Border>

            <!-- Bảo mật & Kho (tím nhạt) -->
            <Border Grid.Row="1" Grid.Column="1" Style="{StaticResource Section}" Background="#F3EAFE" Margin="10,10,0,0">
                <StackPanel>
                    <TextBlock Text="⚙  Bảo mật &amp; Kho" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="{StaticResource ColText}"/>
                    <TextBlock Text="Timeout phiên (phút)" Foreground="{StaticResource ColSub}" />
                    <TextBox Text="{Binding TimeoutPhien}" />
                    <TextBlock Text="Ngưỡng tồn kho thấp" Foreground="{StaticResource ColSub}" />
                    <TextBox Text="{Binding NguongTonKhoThap}" />
                </StackPanel>
            </Border>
        </Grid>

        <!-- Save -->
        <DockPanel Grid.Row="3">
            <Button Content="Lưu cấu hình"
                    DockPanel.Dock="Right"
                    Width="140" Height="36"
                    Background="#0FBF84" Foreground="White"
                    BorderThickness="0"
                    Command="{Binding CmdSave}"
                    Cursor="Hand"/>
        </DockPanel>
    </Grid>
</UserControl>

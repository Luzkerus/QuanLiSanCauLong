<UserControl x:Class="QuanLiSanCauLong.LopTrinhBay.ManHinh.TongQuan.ucTongQuan"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="700" d:DesignWidth="1080"
             Background="#F6FDF9">

    <!-- =================== RESOURCES =================== -->
    <UserControl.Resources>
        <!-- Palette -->
        <SolidColorBrush x:Key="ColText"     Color="#21382D"/>
        <SolidColorBrush x:Key="ColSub"      Color="#6B7E74"/>
        <SolidColorBrush x:Key="ColBorder"   Color="#E1F0E9"/>
        <SolidColorBrush x:Key="ColCard"     Color="White"/>
        <SolidColorBrush x:Key="ColAccent"   Color="#2BB673"/>
        <SolidColorBrush x:Key="ColAccentDark" Color="#0D7049"/>

        <!-- Card thường (giữ để dùng chỗ khác) -->
        <Style x:Key="CardBorder" TargetType="Border">
            <Setter Property="CornerRadius" Value="14"/>
            <Setter Property="Background" Value="{StaticResource ColCard}"/>
            <Setter Property="BorderBrush" Value="{StaticResource ColBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.15"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- KPI CARD: giống ucThanhToan (shadow + scale on hover) -->
        <Style x:Key="KpiCardStyle" TargetType="Border" BasedOn="{StaticResource CardBorder}">
            <Setter Property="CornerRadius" Value="16"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="18"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="18" ShadowDepth="0" Opacity="0.18"/>
                </Setter.Value>
            </Setter>
            <Setter Property="RenderTransformOrigin" Value="0.5,0.5"/>
            <Setter Property="RenderTransform">
                <Setter.Value>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Effect">
                        <Setter.Value>
                            <DropShadowEffect BlurRadius="24" ShadowDepth="0" Opacity="0.28"/>
                        </Setter.Value>
                    </Setter>
                    <Setter Property="RenderTransform">
                        <Setter.Value>
                            <ScaleTransform ScaleX="1.02" ScaleY="1.02"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Typography KPI -->
        <Style x:Key="KpiTitle" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource ColSub}"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="KpiValue" TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource ColText}"/>
            <Setter Property="FontSize" Value="26"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <!-- Badge nhỏ -->
        <Style x:Key="Badge" TargetType="Border">
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="10,3"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Nút solid -->
        <Style x:Key="BtnSolid" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Background" Value="{StaticResource ColAccent}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="6" ShadowDepth="1" Opacity="0.18"/>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource ColAccentDark}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Nút viền -->
        <Style x:Key="BtnOutline" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource ColAccent}"/>
            <Setter Property="BorderThickness" Value="1.2"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#EAF6EF"/>
                    <Setter Property="BorderBrush" Value="#0D7049"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Trạng thái sân -->
        <Style x:Key="TrangThaiBorderStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding TrangThai}" Value="Đang chơi">
                    <Setter Property="Background" Value="#DFF7EA"/>
                    <Setter Property="BorderBrush" Value="#1E7F4C"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding TrangThai}" Value="Trống">
                    <Setter Property="Background" Value="#EDF3FF"/>
                    <Setter Property="BorderBrush" Value="#3C5AA8"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding TrangThai}" Value="Đã đặt">
                    <Setter Property="Background" Value="#FFF4E3"/>
                    <Setter Property="BorderBrush" Value="#B76B00"/>
                </DataTrigger>
                <DataTrigger Binding="{Binding TrangThai}" Value="Bảo trì">
                    <Setter Property="Background" Value="#FFE5E5"/>
                    <Setter Property="BorderBrush" Value="#CC0000"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <!-- =================== LAYOUT =================== -->
    <Grid Margin="16,10,16,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title + Buttons -->
        <Grid Grid.Row="0" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical" VerticalAlignment="Center" Grid.Column="0" Margin="0,0,12,0">
                <TextBlock Text="Tổng quan hệ thống" FontSize="24" FontWeight="Bold" Foreground="{StaticResource ColText}"/>
                <TextBlock Text="Chào mừng trở lại! Đây là tổng quan hoạt động hôm nay." FontWeight="SemiBold"
                           Foreground="{StaticResource ColSub}" FontSize="12"/>
            </StackPanel>

            <!-- Xuất báo cáo -->
            <Border Margin="0,0,10,0" CornerRadius="6"
                    Background="#E0E0E0" Cursor="Hand" Grid.Column="1">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#E0E0E0"/>
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect BlurRadius="0" Opacity="0"/>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#EFECE3"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="6" Opacity="0.3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="Xuất báo cáo"
                           FontSize="14"
                           Foreground="Black"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Center"
                           Padding="14,8"/>
            </Border>

            <!-- Đặt sân mới -->
            <Border Grid.Column="2"
        x:Name="btnMoDatSan"
        Cursor="Hand"
        CornerRadius="8"
        Background="#2BB673"
        MouseLeftButtonUp="btnMoDatSan_MouseLeftButtonUp">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="#2BB673"/>
                        <Setter Property="Effect">
                            <Setter.Value>
                                <DropShadowEffect BlurRadius="4" ShadowDepth="0" Opacity="0.2"/>
                            </Setter.Value>
                        </Setter>
                        <Setter Property="Padding" Value="14,8"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0D7049"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="8" ShadowDepth="0" Opacity="0.4"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <TextBlock Text="Đặt sân mới"
               FontSize="14"
               FontWeight="SemiBold"
               Foreground="White"
               VerticalAlignment="Center"
               HorizontalAlignment="Center"/>
            </Border>

        </Grid>

        <!-- Body -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>

                <!-- KPI 4 thẻ: áp dụng KpiCardStyle như ucThanhToan -->
                <UniformGrid Columns="4" Margin="0,0,0,12">
                    <!-- Doanh thu hôm nay -->
                    <Border Style="{StaticResource KpiCardStyle}" Margin="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Doanh thu hôm nay" Style="{StaticResource KpiTitle}"/>
                                <TextBlock Text="{Binding DoanhThuHomNay, StringFormat={}{0:N0}đ}" Style="{StaticResource KpiValue}"/>
                                <TextBlock Text="{Binding BienDongDoanhThu}" Foreground="#16A34A" Margin="0,6,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Background="#E8FBF2" CornerRadius="12" Padding="10" Margin="8,0,0,28">
                                <TextBlock Text="💵" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Số lượt đặt sân -->
                    <Border Style="{StaticResource KpiCardStyle}" Margin="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Số lượt đặt sân" Style="{StaticResource KpiTitle}"/>
                                <TextBlock Text="{Binding SoLuotDatSan}" Style="{StaticResource KpiValue}"/>
                                <TextBlock Text="{Binding BienDongLuotDat}" Foreground="#16A34A" Margin="0,6,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Background="#FFF3E6" CornerRadius="12" Padding="10" Margin="8,0,0,28">
                                <TextBlock Text="🗓️" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Hội viên mới -->
                    <Border Style="{StaticResource KpiCardStyle}" Margin="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Hội viên mới" Style="{StaticResource KpiTitle}"/>
                                <TextBlock Text="{Binding HoiVienMoi}" Style="{StaticResource KpiValue}"/>
                                <TextBlock Text="{Binding BienDongHoiVien}" Foreground="#16A34A" Margin="0,6,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Background="#ECF3FF" CornerRadius="12" Padding="10" Margin="8,0,0,28">
                                <TextBlock Text="👥" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>

                    <!-- Tỉ lệ lấp đầy -->
                    <Border Style="{StaticResource KpiCardStyle}" Margin="8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="Tỉ lệ lấp đầy" Style="{StaticResource KpiTitle}"/>
                                <TextBlock Text="{Binding TiLeLapDay, StringFormat={}{0:P0}}" Style="{StaticResource KpiValue}"/>
                                <TextBlock Text="{Binding BienDongLapDay}" Foreground="#DC2626" Margin="0,6,0,0"/>
                            </StackPanel>
                            <Border Grid.Column="1" Background="#F5ECFF" CornerRadius="12" Padding="10" Margin="8,0,0,28">
                                <TextBlock Text="📈" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                            </Border>
                        </Grid>
                    </Border>
                </UniformGrid>

                <!-- Tình trạng sân + Top hội viên -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Tình trạng sân -->
                    <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="8">
                        <StackPanel>
                            <DockPanel Margin="0,0,0,8">
                                <TextBlock Text="Tình trạng sân" FontWeight="SemiBold"/>
                            </DockPanel>

                            <ItemsControl ItemsSource="{Binding TinhTrangSan}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Columns="3"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Style="{StaticResource CardBorder}" Padding="12" Margin="8">
                                            <StackPanel>
                                                <TextBlock Text="{Binding TenSan}" FontWeight="SemiBold"/>
                                                <Border CornerRadius="10" Padding="8,2" Margin="0,6,0,0"
                                                        Style="{StaticResource TrangThaiBorderStyle}">
                                                    <TextBlock Text="{Binding TrangThai}" FontSize="12" FontWeight="SemiBold"/>
                                                </Border>
                                                <TextBlock Text="{Binding Den}" Foreground="#6B7E74" Margin="0,6,0,0" FontSize="12"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>

                    <!-- Top hội viên -->
                    <Border Grid.Column="1" Style="{StaticResource CardBorder}" Margin="8">
                        <StackPanel>
                            <DockPanel Margin="0,0,0,8">
                                <TextBlock Text="⭐" DockPanel.Dock="Left" Margin="0,0,6,0"/>
                                <TextBlock Text="Top hội viên" FontWeight="SemiBold"/>
                            </DockPanel>

                            <ItemsControl ItemsSource="{Binding TopHoiVien}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <DockPanel Margin="0,6,0,6">
                                            <Border Width="36" Height="36" CornerRadius="18" Background="#F6F8F7" DockPanel.Dock="Left">
                                                <TextBlock Text="{Binding TenVietTat}" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="SemiBold"/>
                                            </Border>
                                            <StackPanel Margin="10,0,0,0">
                                                <TextBlock Text="{Binding HoTen}"/>
                                                <TextBlock Text="{Binding LuotChoi, StringFormat={}{0} lượt chơi}" Foreground="#6B7E74" FontSize="12"/>
                                            </StackPanel>
                                            <TextBlock Text="{Binding Diem, StringFormat={}{0} điểm}" DockPanel.Dock="Right" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                                        </DockPanel>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Lịch đặt sân gần đây -->
                <Border Style="{StaticResource CardBorder}" Margin="8">
                    <StackPanel>
                        <DockPanel Margin="0,0,0,8">
                            <TextBlock Text="📅" DockPanel.Dock="Left" Margin="0,0,6,0"/>
                            <TextBlock Text="Lịch đặt sân gần đây" FontWeight="SemiBold"/>
                        </DockPanel>

                        <ListView ItemsSource="{Binding LichDatGanDay}" BorderThickness="0">
                            <ListView.View>
                                <GridView AllowsColumnReorder="False">
                                    <GridViewColumn Header="Sân" DisplayMemberBinding="{Binding TenSan}" Width="auto"/>
                                    <GridViewColumn Header="Khách hàng" DisplayMemberBinding="{Binding KhachHang}" Width="auto"/>
                                    <GridViewColumn Header="Thời gian" DisplayMemberBinding="{Binding KhoangGio}" Width="auto"/>
                                    <GridViewColumn Header="Trạng thái" Width="auto">
                                        <GridViewColumn.CellTemplate>
                                            <DataTemplate>
                                                <Border CornerRadius="10" Padding="8,2" Style="{StaticResource TrangThaiBorderStyle}">
                                                    <TextBlock Text="{Binding TrangThai}" FontSize="12" FontWeight="SemiBold"/>
                                                </Border>
                                            </DataTemplate>
                                        </GridViewColumn.CellTemplate>
                                    </GridViewColumn>
                                </GridView>
                            </ListView.View>
                        </ListView>
                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>

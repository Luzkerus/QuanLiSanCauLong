<UserControl x:Class="QuanLiSanCauLong.LopTrinhBay.ManHinh.Controls.ucHeader"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             mc:Ignorable="d"
             d:DesignHeight="56" d:DesignWidth="1200"
             x:Name="Root"
             Background="White">

    <!-- Header chính -->
    <Grid Height="56">
        <Grid.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                <!-- Xanh pastel nâng tone -->
                <GradientStop Color="#C9FFE9" Offset="0"/>

                <!-- Vàng pastel nâng tone -->
                <GradientStop Color="#FFF4C2" Offset="1"/>
            </LinearGradientBrush>
        </Grid.Background>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Ngày tháng -->
        <StackPanel Grid.Column="1"
                Orientation="Horizontal"
                VerticalAlignment="Center"
                Margin="12,0,16,0">

            <TextBlock x:Name="txtDate"
                   Foreground="#495a54"
                   FontSize="13"
                   VerticalAlignment="Center"
                   FontWeight="Medium"/>
        </StackPanel>
    </Grid>

</UserControl>

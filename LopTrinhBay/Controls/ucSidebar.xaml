<UserControl x:Class="QuanLiSanCauLong.LopTrinhBay.ManHinh.Controls.ucSidebar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="720" d:DesignWidth="220"
             x:Name="Root"
             Width="220" Background="White">

    <!-- ============= Resources (giữ màu & style giống mẫu) ============= -->
    <UserControl.Resources>
        <!-- Nếu app đã có các brush này, có thể bỏ 4 dòng dưới để dùng chung -->
        <SolidColorBrush x:Key="ColText"   Color="#21382D"/>
        <SolidColorBrush x:Key="ColBorder" Color="#E6EFEA"/>
        <SolidColorBrush x:Key="ColActiveBg" Color="#0FBF84"/>
        <SolidColorBrush x:Key="ColActiveFg" Color="White"/>

        <!-- Style: Item đang active -->
        <Style x:Key="MenuItemActive" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Background" Value="{StaticResource ColActiveBg}"/>
            <Setter Property="BorderBrush" Value="{StaticResource ColActiveBg}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="6,4,6,0"/>
            <Setter Property="Padding" Value="10,8"/>
        </Style>

        <!-- Style: Item bình thường -->
        <Style x:Key="MenuItemNormal" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="{StaticResource ColBorder}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="6,4,6,0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F0FFFA"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <!-- ===== Sidebar (fixed / negative) ===== -->
    <Border Background="White" BorderBrush="{StaticResource ColBorder}" BorderThickness="0,0,1,0">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Logo -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="16" VerticalAlignment="Top">
                <Ellipse Width="28" Height="28" Fill="#2BB673"/>
                <StackPanel Margin="8,0,0,0">
                    <TextBlock Text="Sân Cầu Lông" FontSize="16" FontWeight="SemiBold"/>
                    <TextBlock Text="Củ Chi" Foreground="#888" FontSize="14"/>
                </StackPanel>
            </StackPanel>

            <!-- Menu -->
            <StackPanel Grid.Row="1" Margin="4,0,4,0">
                <!-- Active: Tổng quan -->

                <Border x:Name="Item_Overview" Style="{StaticResource MenuItemNormal}" Tag="overview"
                        MouseLeftButtonDown="MenuItem_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📊" Margin="4,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Tổng quan" FontSize="14" Foreground="{StaticResource ColText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border x:Name="Item_Courts" Style="{StaticResource MenuItemNormal}" Tag="courts"
                        MouseLeftButtonDown="MenuItem_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🏆" Margin="4,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Quản lý sân" FontSize="14" Foreground="{StaticResource ColText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border x:Name="Item_Booking" Style="{StaticResource MenuItemNormal}" Tag="booking"
                        MouseLeftButtonDown="MenuItem_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📅" Margin="4,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Đặt sân" FontSize="14" Foreground="{StaticResource ColText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border x:Name="Item_Customers" Style="{StaticResource MenuItemNormal}" Tag="customers"
                        MouseLeftButtonDown="MenuItem_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="👤" Margin="4,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Khách hàng" FontSize="14" Foreground="{StaticResource ColText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border x:Name="Item_Payment" Style="{StaticResource MenuItemNormal}" Tag="payment"
                        MouseLeftButtonDown="MenuItem_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💳" Margin="4,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Thanh toán" FontSize="14" Foreground="{StaticResource ColText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border x:Name="Item_POS" Style="{StaticResource MenuItemNormal}" Tag="pos"
                        MouseLeftButtonDown="MenuItem_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🧾" Margin="4,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Kho &amp; POS" FontSize="14" Foreground="{StaticResource ColText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border x:Name="Item_Staff" Style="{StaticResource MenuItemNormal}" Tag="staff"
                        MouseLeftButtonDown="MenuItem_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🧑‍💼" Margin="4,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Nhân viên" FontSize="14" Foreground="{StaticResource ColText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border x:Name="Item_Reports" Style="{StaticResource MenuItemNormal}" Tag="reports"
                        MouseLeftButtonDown="MenuItem_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📈" Margin="4,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Báo cáo" FontSize="14" Foreground="{StaticResource ColText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Border x:Name="Item_Settings" Style="{StaticResource MenuItemNormal}" Tag="settings"
                        MouseLeftButtonDown="MenuItem_Click">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="⚙" Margin="4,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Cài đặt" FontSize="14" Foreground="{StaticResource ColText}" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <!-- Admin -->
            <Border Grid.Row="2" Margin="10,8,10,12" Padding="12"
                    BorderBrush="{StaticResource ColBorder}" BorderThickness="1" CornerRadius="12">
                <DockPanel>
                    <Ellipse Width="32" Height="32" Fill="#2BB673" DockPanel.Dock="Left"/>
                    <StackPanel Margin="10,0,0,0">
                        <TextBlock Text="{Binding AdminName, ElementName=Root, FallbackValue=Admin User}" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding AdminRole, ElementName=Root, FallbackValue=Quản lý}" Foreground="#889" FontSize="12"/>
                    </StackPanel>
                    <Border HorizontalAlignment="Right" VerticalAlignment="Center"
                            CornerRadius="12" BorderThickness="1" BorderBrush="{StaticResource ColBorder}"
                            Padding="6,4" Margin="8,0,0,0">
                        <TextBlock Text="↦" />
                    </Border>
                </DockPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
